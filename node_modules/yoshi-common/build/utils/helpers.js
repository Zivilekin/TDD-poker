"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.requestHttps = exports.isTruthy = void 0;
const https_1 = __importDefault(require("https"));
function isTruthy(x) {
    return x !== undefined && x !== null;
}
exports.isTruthy = isTruthy;
function requestHttps(url, params) {
    const queryParams = Object.entries(params)
        .map(([key, value]) => `${key}=${encodeURIComponent(`${value}`)}`)
        .join('&');
    return new Promise((resolve, reject) => {
        const req = https_1.default.request(`${url}?${queryParams}`, res => {
            if ((res.statusCode && res.statusCode < 200) ||
                (res.statusCode && res.statusCode >= 300)) {
                return reject(`Status code: ${res.statusCode}`);
            }
            res.on('end', resolve);
        });
        req.on('error', reject);
        req.end();
    });
}
exports.requestHttps = requestHttps;
//# sourceMappingURL=helpers.js.map