/// <reference types="node" />
import https from 'https';
import OriginalWebpackDevServer from 'webpack-dev-server';
import webpack from 'webpack';
import express from 'express';
export declare function redirectMiddleware(hostname: string, port: number): express.Handler;
export declare const host = "0.0.0.0";
export declare class WebpackDevServer extends OriginalWebpackDevServer {
    port: number;
    https: boolean | https.ServerOptions;
    compiler: webpack.Compiler;
    suricate: boolean;
    appName: string;
    private suricateApi;
    constructor(compiler: webpack.Compiler, { publicPath, https, port, suricate, appName, cwd, }: {
        publicPath: string;
        https: boolean | https.ServerOptions;
        port: number;
        suricate: boolean;
        appName: string;
        cwd?: string;
    });
    send(type: string, data: any): void;
    listenPromise(): Promise<unknown>;
}
//# sourceMappingURL=webpack-dev-server.d.ts.map