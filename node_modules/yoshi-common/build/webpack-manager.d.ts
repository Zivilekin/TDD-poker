/// <reference types="webpack-dev-server" />
import webpack from 'webpack';
export declare type NamedConfiguration = {
    name: string;
    configs: Array<webpack.Configuration>;
};
export default class WebpackManager {
    private namedConfigs;
    constructor();
    addConfigs(name: string, configs: Array<webpack.Configuration | undefined>): void;
    run(): Promise<{
        getAppData: (configName: string) => {
            configs: Array<webpack.Configuration>;
            stats: Array<webpack.Stats>;
        };
    }>;
}
//# sourceMappingURL=webpack-manager.d.ts.map