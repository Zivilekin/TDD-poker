/// <reference types="node" />
import EventEmitter from 'events';
export default class SocketServer extends EventEmitter {
    hmrPort: number;
    private socket;
    private server;
    private connections;
    constructor({ hmrPort }: {
        hmrPort: number;
    });
    send(message: any): void;
    initialize(): Promise<void>;
    static create(): Promise<SocketServer>;
}
//# sourceMappingURL=socket-server.d.ts.map