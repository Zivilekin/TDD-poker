/// <reference types="node" />
import { EventEmitter } from 'events';
interface StorybookProcessOptions {
    port: number;
}
export default class StorybookProcess extends EventEmitter {
    port: number;
    constructor({ port }: StorybookProcessOptions);
    static create({ port }: StorybookProcessOptions): StorybookProcess;
    start: () => Promise<void>;
    reporter: (middlewareOptions: any, options: any) => void;
}
export {};
//# sourceMappingURL=storybook-process.d.ts.map