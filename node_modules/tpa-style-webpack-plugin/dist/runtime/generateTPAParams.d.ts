import { IOptions } from './main';
import { ISiteColor, ISiteTextPreset, IStyleParams } from './types';
export interface ITPAParams {
    colors: {
        [index: string]: {
            value: string;
        };
    };
    numbers: {
        [index: string]: number;
    };
    booleans: {
        [index: string]: boolean;
    };
    fonts: Object;
    strings: Object;
}
export declare function generateTPAParams(siteColors: ISiteColor[], siteTextPresets: ISiteTextPreset, styleParams: IStyleParams, options: Partial<IOptions>): ITPAParams;
