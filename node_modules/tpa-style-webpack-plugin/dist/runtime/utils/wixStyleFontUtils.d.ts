import { ISiteTextPreset, IStyleFont } from '../types';
export declare const wixStylesFontUtils: {
    getFullFontStyles({ fontStyles, siteTextPresets, }: {
        fontStyles: {
            [s: string]: IStyleFont;
        };
        siteTextPresets: ISiteTextPreset;
    }): any;
    toFontCssValue(value: any): string;
    isStringHack(fontParam: any): boolean;
    isValidFontParam(fontParam: any): boolean;
};
