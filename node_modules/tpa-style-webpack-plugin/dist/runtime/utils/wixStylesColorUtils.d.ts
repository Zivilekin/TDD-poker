import { ISiteColor, IStyleColor } from '../types';
export declare const wixStylesColorUtils: {
    getFullColorStyles({ colorStyles, siteColors }: {
        siteColors: ISiteColor[];
        colorStyles: {
            [key: string]: IStyleColor;
        };
    }): any;
};
