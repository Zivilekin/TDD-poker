"use strict";
/**
 * @fileoverview Utility functions for wix-style-react version configuration.
 * @author Artem Yavorsky
 */
Object.defineProperty(exports, "__esModule", { value: true });
var resolve = require("resolve");
var semver = require("semver");
function detectVersion(packageName) {
    try {
        var pkgPath = resolve.sync(packageName + "/package.json", { basedir: process.cwd() });
        var pkg = require(pkgPath);
        return pkg.version;
    }
    catch (e) {
        return null;
    }
}
exports.detectVersion = detectVersion;
function isVersionGreater(packageName, version) {
    var currentVersion = detectVersion(packageName);
    if (!currentVersion)
        return false;
    return semver.gt(currentVersion, version);
}
exports.isVersionGreater = isVersionGreater;
;
//# sourceMappingURL=utils.js.map